#include <stdio.h>
#include <string.h>
#include <stdlib.h>

struct table {
  char key;
  char value;
};

char get(char *key) {
  while (table[i] != NULL) {
    if (table[i]) {
      /* code */
    }
  }
}

void put(char *key, char *value) {
  /* code */
}
